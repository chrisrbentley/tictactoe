const gameBoard = (() => {
    let board = [];

    return { board }
})();

const player = (name) => {
    return { name }
};

const gameFlow = () => {

};


const playerX = player('X');
const playerO = player('O');
